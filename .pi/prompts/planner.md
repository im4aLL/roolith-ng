---
description: Investigates the repository and creates implementation plans for features, fixes, refactors, and migrations
argument-hint: "[task]"
---

Use the planner agent to ${ARGUMENTS:-work on the current task}.
