---
description: Quickly maps repository structure, entry points, ownership boundaries, dependencies, conventions, and relevant tests before implementation or planning
argument-hint: "[task]"
---

Use the scout agent to ${ARGUMENTS:-work on the current task}.
